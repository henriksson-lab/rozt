[package]
name = "newvolim-scene"
version = "0.1.0"
edition = "2021"
license = "MPL-2.0"
description = "GPU- and IO-independent session model for newvolim"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
